# node-test1

Minimal Express app exposing GET /hello?name={name} returning plain text greeting.

Run locally:

```bash
npm install
npm start
```

Test:

```bash
curl "http://localhost:3000/hello?name=Alice"
# -> Hello, Alice
```
# node-test1